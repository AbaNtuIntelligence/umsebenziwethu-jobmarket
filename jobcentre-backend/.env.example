DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# Leave blank locally to use SQLite. Set this on Render/Axxess for PostgreSQL.
DATABASE_URL=
FRONTEND_URL=http://localhost:5173
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=Job Centre <noreply@localhost>
# Set this in the deployed private pilot. Leave blank for unrestricted local testing.
PILOT_INVITE_CODE=
