import {useState} from "react";
import {useNavigate} from "react-router-dom";
import api,{errorMessage} from "../services/api";
import {useAuth} from "../state/AuthContext";
export default function SettingsPage(){const {user,setUser,logout}=useAuth();const navigate=useNavigate();const [open,setOpen]=useState(false);const [message,setMessage]=useState("");async function preferences(e){e.preventDefault();const form=new FormData(e.currentTarget);const payload={email_notifications:form.has("email_notifications"),sms_notifications:form.has("sms_notifications"),whatsapp_notifications:form.has("whatsapp_notifications")};const {data}=await api.patch("/auth/me/",payload);setUser(data);setMessage("Notification preferences saved.");}async function remove(e){e.preventDefault();try{await api.post("/auth/delete-account/",Object.fromEntries(new FormData(e.currentTarget)));logout();navigate("/");}catch(e){setMessage(errorMessage(e));}}return <div className="form-card"><h1>Account settings</h1>{message&&<div className="alert">{message}</div>}<section className="settings-section"><h2>Notification preferences</h2><form className="preference-list" onSubmit={preferences}><label><input type="checkbox" name="email_notifications" defaultChecked={user.email_notifications}/>Transactional email</label><label><input type="checkbox" name="sms_notifications" defaultChecked={user.sms_notifications}/>SMS when available</label><label><input type="checkbox" name="whatsapp_notifications" defaultChecked={user.whatsapp_notifications}/>WhatsApp when available</label><button className="button primary">Save preferences</button></form><p><small>SMS and WhatsApp delivery will activate only after those services are configured.</small></p></section><section className="settings-section"><h2>Privacy</h2><p>You can review the pilot Privacy Notice or permanently remove your account and associated marketplace records.</p><a href="/privacy">Read Privacy Notice</a></section><section className="danger-zone"><h2>Delete account</h2><p>This permanently deletes your profile, jobs, applications and saved items. This cannot be undone.</p>{!open?<button className="button danger" onClick={()=>setOpen(true)}>Delete my account</button>:<form className="form-stack" onSubmit={remove}>{message&&<div className="alert error">{message}</div>}<label>Current password<input type="password" name="password" required/></label><label>Type DELETE<input name="confirm" required/></label><button className="button danger">Permanently delete account</button></form>}</section></div>}
