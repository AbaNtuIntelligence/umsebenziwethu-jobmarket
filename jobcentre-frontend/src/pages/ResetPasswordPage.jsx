import {useState} from "react";
import {useNavigate,useParams} from "react-router-dom";
import api,{errorMessage} from "../services/api";
export default function ResetPasswordPage(){const {uid,token}=useParams();const navigate=useNavigate();const [error,setError]=useState("");async function submit(e){e.preventDefault();const form=Object.fromEntries(new FormData(e.currentTarget));if(form.new_password!==form.confirm_password)return setError("Passwords do not match.");try{await api.post("/auth/password-reset-confirm/",{uid,token,new_password:form.new_password});navigate("/login");}catch(e){setError(errorMessage(e));}}return <div className="auth-card"><h1>Choose a new password</h1>{error&&<div className="alert error">{error}</div>}<form className="form-stack" onSubmit={submit}><label>New password<input type="password" name="new_password" minLength="8" required/></label><label>Confirm password<input type="password" name="confirm_password" minLength="8" required/></label><button className="button primary">Update password</button></form></div>}
